USE typologie;
CREATE TABLE press (
    id INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    name TEXT,
    dossier TEXT,
    date TIMESTAMP,
    archived INT
);
INSERT INTO users (firstname, lastname, mail, address, password, role, valid, archived) VALUES('press', 'press','press', '', 'YJEU?2kB', 1, 1, 0);
